# 后端项目架构与项目结构规范说明

> 本文基于对当前代码库（`D:\ideaWork\zy`）的实际探查整理，描述后端工程的技术栈、模块划分、包结构、分层与命名规范，以及本项目在 Snowy 框架上做的定制扩展。
>
> 生成日期：2026-09-16

---

## 一、项目总体定位

本项目基于开源快速开发框架 **Snowy（SnowyAdmin）3.0.0**（groupId：`vip.xiaonuo`）二次开发，是一个 **Spring Boot 3 + 插件化** 的后台服务。在框架自带的系统/鉴权/开发工具等能力之上，本项目新增了一个 **AI 画布 / 短剧创作业务插件 `snowy-plugin-canvas`**（代码包名前缀 `Zy`，数据表前缀 `zy_`），用于短剧项目、分镜、素材、团队、智能体对话及 AI 文/图/视频生成任务的管理。

- 仓库根 `pom.xml` 聚合模块：`snowy-common`、`snowy-plugin`、`snowy-plugin-api`、`snowy-web-app`
- 配套前端目录：`snowy-admin-web`（管理端）、`open-ai-canvas`（画布前端）

---

## 二、技术栈一览

| 分类 | 技术 / 版本 | 说明 |
|------|-------------|------|
| 语言 / 运行时 | Java 17 | `java.version=17` |
| 基础框架 | Spring Boot 3.5.9 / Spring Framework 6.2.x | `spring-boot-starter-parent` |
| ORM | MyBatis-Plus 3.5.5 | 实体/Mapper/分页/逻辑删除 |
| 数据源 | dynamic-datasource 4.3.1 + Druid 1.2.21 | 多数据源、连接池、监控台 |
| 数据库 | PostgreSQL（**当前实际启用**） | 连接 `localhost:5432/zy-new`；代码同时兼容 MySQL / Oracle / MSSQL / 达梦 / 人大金仓（注释切换） |
| 缓存 / 分布式 | Redis + Redisson 3.45 | `spring.data.redis`，database=1 |
| 鉴权 | Sa-Token 1.44 | B 端 `StpUtil` 与 C 端 `StpClientUtil` 双体系隔离，支持 SSO/OAuth2/JWT |
| API 文档 | Knife4j 4.5（OpenAPI3 / springdoc） | 按插件分包分组，`/doc.html` |
| 工具库 | Hutool 5.8.25、Lombok、easy-trans、ip2region、pinyin4j | — |
| Excel | EasyExcel 3.3.3 / EasyPOI 4.4.0 | 导入导出 |
| 国密 | sm-crypto 0.3.2、BouncyCastle | SM2 登录加密、SM3 密码摘要、SM4 字段加解密 |
| 文件存储 | x-file-storage 2.1.0（本地/OSS/COS/MinIO/S3） | 本地默认 `./uploads` |
| AI 网关 | 自研 `zyapi` 层对接 NewApi / ComfyUI | 文/图/视频生成异步任务 |
| 服务端口 | `server.port=82`，应用名 `snowy` | SSE 异步超时 10 分钟（分镜/视频任务监听） |

---

## 三、Maven 模块结构

根 `pom.xml` 为聚合 POM（`packaging=pom`），统一 `dependencyManagement` 版本。四个一级模块职责如下：

```
snowy (root, pom)
│
├── snowy-common            基础通用模块（无业务，被所有插件依赖）
│     包根：vip.xiaonuo.common
│
├── snowy-plugin             业务插件聚合（packaging=pom）
│     ├── snowy-plugin-auth      登录鉴权插件
│     ├── snowy-plugin-biz       业务功能插件
│     ├── snowy-plugin-client    C 端功能插件
│     ├── snowy-plugin-dev       开发工具插件（配置/字典/文件/定时/日志/消息）
│     ├── snowy-plugin-gen       代码生成插件
│     ├── snowy-plugin-mobile    移动端管理插件
│     ├── snowy-plugin-sys       系统功能插件（用户/组织/角色/菜单/按钮）
│     └── snowy-plugin-canvas    ★本项目定制：AI 画布/短剧业务插件
│
├── snowy-plugin-api         插件对外 API 接口聚合（packaging=pom）
│     └── snowy-plugin-*-api     各插件暴露给其他插件调用的接口（provider）
│
└── snowy-web-app            主启动模块（jar，最终打包运行）
      包根：vip.xiaonuo.core / vip.xiaonuo.Application
      依赖全部 snowy-plugin-* 插件
```

**模块依赖方向**（单向、无环）：

```
snowy-web-app ──► 各 snowy-plugin-* （业务插件）
各插件        ──► snowy-plugin-*-api （跨插件调用时依赖对方 api）
所有插件      ──► snowy-common      （基础工具/基类/注解）
```

> 插件之间**不直接依赖实现模块**，需要跨插件调用时，只依赖对方的 `*-api` 接口模块，调用方通过接口 + 实现方提供的 `provider` 完成解耦。

---

## 四、启动模块 snowy-web-app

- 启动类：`vip.xiaonuo.Application`（`@SpringBootApplication`，位于 `vip.xiaonuo` 根包，天然扫描 `vip.xiaonuo.**`）
- 资源目录 `src/main/resources`：
  - `application.properties` — 主配置（数据源、Redis、MyBatis-Plus、Sa-Token、Knife4j、任务 worker 等）
  - `application-*.yml` — 多环境（local/test/prod/docker）
  - `logback-spring.xml` — 日志
  - `_sql/snowy_mysql.sql` — 建表脚本（打包时排除）
- `vip.xiaonuo.core` 下：
  - `config/`：`GlobalConfigure`（核心全局配置）、`JacksonConfigure`、`Knife4jConfigure`、`DruidConfigure`、`RedissonConfig`
  - `handler/`：`GlobalExceptionHandler`、`GlobalErrorViewHandler` 等全局异常/错误处理
  - `listener/`：`ResourceCollectListener`

### GlobalConfigure 全局机制要点

- `@MapperScan(basePackages = {"vip.xiaonuo.**.mapper"})`：统一扫描所有插件 Mapper
- **Sa-Token 全局过滤器**：区分三类路由
  - 无需登录白名单（登录、文档、静态资源、`/public/**` 等）
  - B 端接口 → `StpUtil.checkLogin()`；超管接口再校验内置超管角色
  - C 端接口（`/auth/c/**`、`/client/**` 及 canvas 的 `/projects`、`/tasks`、`/assets` 等）→ `StpClientUtil.checkLogin()`
- 跨域 CORS 响应头、OPTIONS 预检放行
- MyBatis-Plus 分页插件 `PaginationInnerInterceptor`
- **公共字段自动填充** `MetaObjectHandler`：insert 时填 `deleteFlag/createUser/createTime`，update 时填 `updateUser/updateTime`
- 自定义 SQL 注入器：增加物理删除方法 `deleteAbsoluteById`
- AOP：`@CommonNoRepeat` 防重复提交、`@CommonWrapper` 出参包装
- 数据变化事件中心 `CommonDataChangeEventCenter`

---

## 五、插件内部包结构规范

每个业务插件根包为 `vip.xiaonuo.<插件名>`，例如 sys 插件是 `vip.xiaonuo.sys`，canvas 插件是 `vip.xiaonuo.canvas`。插件内统一分为 **core** 与 **modular** 两大部分：

```
vip.xiaonuo.<plugin>/
├── core/                      插件级“横切”能力，不属于某个业务域
│   ├── config/                插件自己的 Spring 配置（@Configuration / @ComponentScan）
│   ├── enums/                 插件级枚举
│   ├── listener/              事件监听器
│   ├── timer/                 定时任务（xxl/自实现 timer runner）
│   └── util/                  插件内工具类
│
└── modular/                   业务模块，按业务域（domain）再分包
    └── <domain>/              例：shot / asset / project / user ...
        ├── controller/        REST 控制器
        ├── service/           业务接口（XxxService extends IService<Entity>）
        │   └── impl/          业务实现（XxxServiceImpl extends ServiceImpl<Mapper, Entity>）
        ├── mapper/            MyBatis-Plus Mapper 接口（XxxMapper extends BaseMapper<Entity>）
        │   └── mapping/       Mapper XML（XxxMapper.xml）
        ├── entity/            数据库实体（Xxx，@TableName）
        ├── param/             入参对象（XxxAddParam / XxxEditParam / XxxPageParam / XxxIdParam）
        ├── enums/             业务域枚举（可选）
        ├── result/            出参包装对象（按需，如 sys 插件使用）
        └── provider/          跨插件对外暴露的实现（仅需要被别的插件调用时，如 sys/user）
```

### 分层调用链

```
HTTP 请求
  └─ Controller（参数校验、鉴权注解、CommonLog 操作日志、返回 CommonResult）
       └─ Service（业务编排、事务 @Transactional、数据范围校验）
            └─ Mapper（MyBatis-Plus / XML）
                 └─ 数据库
```

---

## 六、代码命名与编写规范（实测约定）

以 canvas 插件 `shot`（短剧分镜）业务域为样例，可直接照此模板新建业务：

| 层 | 类名样例 | 关键写法 |
|----|----------|----------|
| 实体 | `ZyShot` | `@Getter @Setter @TableName("zy_shot")`；主键 `@TableId`（String，雪花 ID）；`@TableLogic deleteFlag`；审计字段 `createUser/createTime/updateUser/updateTime` 用 `@TableField(fill=...)` |
| Mapper | `ZyShotMapper` | 继承 `BaseMapper<ZyShot>`；XML 放同包 `mapping/` 下 |
| Service | `ZyShotService` | 继承 `IService<ZyShot>`；方法含 `page/add/edit/delete/detail/queryEntity` 及导入导出 |
| ServiceImpl | `ZyShotServiceImpl` | 继承 `ServiceImpl<ZyShotMapper, ZyShot>`，加 `@Service`；写操作方法加 `@Transactional(rollbackFor = Exception.class)` |
| Controller | `ZyShotController` | `@Tag @RestController @Validated`；注入用 `@Resource` |
| 入参 | `ZyShotAddParam` / `ZyShotEditParam` / `ZyShotPageParam` / `ZyShotIdParam` | `@Getter @Setter`；字段用 `@Schema` + JSR-303（`@NotBlank/@NotNull`） |
| 枚举 | `ZyShotEnum` | 业务状态/字典枚举 |

**REST 与 URL 约定**

- 查询类用 `GET`，写操作用 `POST`；统一返回 `CommonResult<T>`（`CommonResult.ok()` / `CommonResult.data(...)`）
- 路径形如 `/canvas/shot/page`、`/canvas/shot/add`、`/canvas/shot/edit`、`/canvas/shot/delete`、`/canvas/shot/detail`、`/canvas/shot/exportData`
- **权限码与 URL 字符串保持一致**：方法上 `@SaCheckPermission("/canvas/shot/add")`，写操作再加 `@CommonLog("添加短剧项目分镜")`
- 列表页统一继承/使用 `CommonPageRequest.defaultPage()` 分页；排序字段/方向用 `CommonSortOrderEnum.validate(...)`
- 查询条件用 `QueryWrapper.checkSqlInjection()` 防注入；动态条件用 lambda 写法
- 业务对象拷贝用 `BeanUtil.toBean(param, Entity.class)`
- 业务异常统一 `throw new CommonException("...")`

**实体通用字段**（每张业务表建议具备）：`id`、`deleteFlag`、`createUser`、`createTime`、`updateUser`、`updateTime`，前两/四个由 `MetaObjectHandler` 自动填充，无需手动 set。

---

## 七、本项目定制部分：snowy-plugin-canvas

### 7.1 包结构

```
vip.xiaonuo.canvas/
├── core/
│   └── config/CanvasConfigure.java     @ComponentScan("vip.xiaonuo.canvas")
│
├── modular/                            标准业务域 CRUD（B 端管理后台风格）
│   ├── agentchat/  asset/  assetcandidate/  assetfolder/
│   ├── canvasunitlink/  dramaproject/  generationtask/  model/
│   ├── project/  session/  share/  shot/  shotartifact/
│   ├── shotrevision/  skill/  team/  teammember/  unit/  workflow/
│
└── zyapi/                              ★C 端/前端 API + AI 能力集成层（ZyApi 前缀）
    ├── controller/      ZyApi*Controller（面向画布前端的业务接口）
    ├── service/ + impl/
    ├── param/  result/
    ├── constant/        TaskConstants 等
    ├── config/          TaskWorkerConfig（task.worker.*）
    ├── client/          NewApiClient（上游模型网关）
    │   ├── adapter/     AbstractMediaAdapter → NewApiMediaAdapter / ComfyUIMediaAdapter（媒体适配，多供应商可插拔）
    │   ├── request/     NewApiImageRequest / NewApiVideoRequest
    │   └── response/    NewApiTaskResponse / NewApiStatusResponse
    └── task/            异步生成任务体系
        ├── TaskProcessor / TaskProcessorRegistry        任务处理器注册表
        ├── processor/     TextGeneration / ImageGeneration / VideoGeneration / StoryboardRows
        ├── polling/       TaskPollingService / TaskResultHandler（轮询上游结果）
        ├── timer/          TaskQueueTimerTaskRunner（定时调度）
        ├── TaskWorkerCoordinator / TaskCompletionNotifier
```

### 7.2 业务域（数据模型）关系

- **dramaproject / project**：短剧项目
  - **unit**：章节 / 分镜单元（canvasunitlink 关联单元连线）
    - **shot**：分镜 → **shotrevision**（分镜版本）、**shotartifact**（分镜产物）
- **asset / assetcandidate / assetfolder**：素材库 / 候选素材 / 素材文件夹
- **team / teammember**：团队与成员
- **agentchat / session**：AI 智能体对话与会话
- **model / generationtask**：可用模型目录与生成任务
- **workflow / skill / share**：工作流、技能、分享

### 7.3 AI 异步任务机制

- 前端提交生成任务 → 落库 `generationtask` → `TaskWorkerCoordinator` 按 `task.worker.concurrency` 调度
- 按任务类型路由到对应 `TaskProcessor`（文/图/视频）
- 通过 `client` 调用上游（NewApi 兼容 OpenAI 风格；ComfyUI 走独立 adapter）
- `polling` 定时轮询上游状态（`poll-interval`，长任务 `poll-timeout-ms` 默认 30 分钟、`lease-timeout` 120s、`max-retries` 3）
- 结果下载落库后经 SSE（`spring.mvc.async.request-timeout=600000`）推送前端

相关配置项（`application.properties`）：

```properties
task.worker.concurrency=50
task.worker.lease-timeout=120
task.worker.poll-interval=2000
task.worker.max-retries=3
```

---

## 八、配置与运行速查

| 项 | 值 / 位置 |
|----|-----------|
| 启动类 | `vip.xiaonuo.Application#main` |
| 端口 | `82`（前端 `snowy-admin-web` 默认 83，画布前端另起） |
| 数据库 | PostgreSQL，库 `zy-new`（见 `application.properties`，可切换 MySQL 等） |
| Redis | `127.0.0.1:6379`，database=1 |
| API 文档 | `http://localhost:82/doc.html`（Knife4j，按插件分组） |
| Druid 监控 | `/druid/**`（admin/123456） |
| Mapper XML 扫描 | `classpath*:vip/xiaonuo/**/mapping/*.xml` |
| 逻辑删除字段 | `DELETE_FLAG`（`DELETED` / `NOT_DELETE`） |
| 主键策略 | `ASSIGN_ID`（雪花算法，String 型） |
| 多环境切换 | `spring.profiles.active=local/test/prod` |

---

## 九、新增一个业务模块的标准步骤（照模板）

以在 canvas 插件新增 `demo` 业务为例：

1. 在 `vip.xiaonuo.canvas.modular.demo` 下建包：`controller / service / impl / mapper / mapping / entity / param / enums`
2. 建实体 `ZyDemo`（`@TableName("zy_demo")`，含审计字段与 `@TableLogic`）
3. 建 `ZyDemoMapper extends BaseMapper<ZyDemo>`，必要时建 `mapping/ZyDemoMapper.xml`
4. 建 `ZyDemoService extends IService<ZyDemo>` 声明 `page/add/edit/delete/detail/queryEntity`
5. 建 `ZyDemoServiceImpl extends ServiceImpl<ZyDemoMapper, ZyDemo>`，写操作加事务与数据范围校验
6. 建入参 `ZyDemoAddParam / EditParam / PageParam / IdParam`，加校验注解
7. 建 `ZyDemoController`，用 `@SaCheckPermission("/canvas/demo/xxx")` + `@CommonLog`，统一返回 `CommonResult`
8. 建表 SQL（`zy_` 前缀），在系统菜单/权限表中登记对应权限码
9. 无需改任何扫描配置：`@MapperScan("vip.xiaonuo.**.mapper")` 与组件扫描会自动生效；若属 C 端接口，按需在 `GlobalConfigure` 的 C 端路由清单中登记路径

---

*说明：本文件由代码实际结构探查生成，框架自带模块（auth/sys/dev/gen 等）的内部业务枚举未逐一展开；如需深入某个具体业务域，可在此基础上继续补充。*
